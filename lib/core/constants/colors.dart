import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0B0C10);
  static const Color surface = Color(0xFF1F2833);
  
  // Accents
  static const Color primary = Color(0xFF00F2FE); // Neon Cyan
  static const Color secondary = Color(0xFF45A29E); // Electric Teal
  
  // Text
  static const Color textPrimary = Color(0xFFC5C6C7);
  static const Color textSecondary = Color(0xFF9EA9B1);
  
  // Functional
  static const Color error = Color(0xFFFF4B2B);
  static const Color success = Color(0xFF00E676);
  
  // Glassmorphism
  static const Color glassBorder = Color(0x33FFFFFF);
  static const Color glassBackground = Color(0x1AFFFFFF);

  // Holographic HUD
  static const Color hudLine = Color(0xFF00F2FE); // targeting rings, brackets, sweep
  static const Color hudGrid = Color(0x1400F2FE); // faint background grid
  static const Color textHud = Color(0xFF9FFFFB); // monospaced readout text
}
